export function parseCsv(text:string){const rows:string[][]=[];let row:string[]=[],field="",quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){field+='"';i++}else quoted=!quoted}else if(c===","&&!quoted){row.push(field.trim());field=""}else if((c==="\n"||c==="\r")&&!quoted){if(c==="\r"&&text[i+1]==="\n")i++;row.push(field.trim());field="";if(row.some(Boolean))rows.push(row);row=[]}else field+=c}row.push(field.trim());if(row.some(Boolean))rows.push(row);return rows}
export function csvRecords(text:string){const [headers,...rows]=parseCsv(text);if(!headers)return[];const normalized=headers.map(h=>h.toLowerCase().replace(/\s+/g,"_"));return rows.map(row=>Object.fromEntries(normalized.map((header,index)=>[header,row[index]??""])))}
