import {createHmac,timingSafeEqual} from "node:crypto";
export function verifySquareSignature(body:string,signature:string,key:string,url:string){const expected=createHmac("sha256",key).update(url+body).digest("base64");const a=Buffer.from(signature),b=Buffer.from(expected);return a.length===b.length&&timingSafeEqual(a,b)}
export async function squareRequest<T>(path:string,init:RequestInit={}){const token=process.env.SQUARE_ACCESS_TOKEN;if(!token)throw new Error("Square is not configured");const base=process.env.SQUARE_ENVIRONMENT==="production"?"https://connect.squareup.com":"https://connect.squareupsandbox.com";const response=await fetch(`${base}${path}`,{...init,headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json",...(init.headers??{})}});const body=await response.json();if(!response.ok)throw new Error(`Square request failed: ${response.status}`);return body as T}
export function createSquarePayment(input:{sourceId:string;amountCents:number;locationId:string;idempotencyKey:string;referenceId:string}){return squareRequest<{payment:{id:string;status:string;order_id?:string}}>("/v2/payments",{method:"POST",body:JSON.stringify({source_id:input.sourceId,idempotency_key:input.idempotencyKey,amount_money:{amount:input.amountCents,currency:"CAD"},location_id:input.locationId,reference_id:input.referenceId,autocomplete:true})})}
export function refundSquarePayment(input:{paymentId:string;amountCents:number;idempotencyKey:string;reason:string}){return squareRequest<{refund:{id:string;status:string}}>("/v2/refunds",{method:"POST",body:JSON.stringify({payment_id:input.paymentId,idempotency_key:input.idempotencyKey,amount_money:{amount:input.amountCents,currency:"CAD"},reason:input.reason})})}
