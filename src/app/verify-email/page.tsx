import Link from "next/link";export default async function Page({searchParams}:{searchParams:Promise<{email?:string}>}){const {email}=await searchParams;return <main className="auth-shell"><section className="auth-card"><Link className="wordmark" href="/">VNDR <span>Hub</span></Link><h1>Check your email</h1><p>We sent a verification link{email?` to ${email}`:""}. Open it to activate your account, then continue to onboarding.</p><p className="switch-link"><Link href="/sign-in">Return to sign in</Link></p></section></main>}
