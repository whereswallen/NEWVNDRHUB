import {createHash} from "node:crypto";import {and,eq,gt,isNull} from "drizzle-orm";import {NextRequest,NextResponse} from "next/server";import {db} from "@/db";import {squareConnections,squareOauthStates} from "@/db/schema";import {encryptSecret} from "@/lib/secrets";import {squareRequestWithToken} from "@/lib/square";
export async function GET(request:NextRequest){const code=request.nextUrl.searchParams.get("code"),state=request.nextUrl.searchParams.get("state");if(!code||!state)return NextResponse.redirect(new URL("/app/integrations?error=oauth",request.url));const hash=createHash("sha256").update(state).digest("hex");const [record]=await db.select().from(squareOauthStates).where(and(eq(squareOauthStates.tokenHash,hash),isNull(squareOauthStates.usedAt),gt(squareOauthStates.expiresAt,new Date()))).limit(1);if(!record)return NextResponse.redirect(new URL("/app/integrations?error=state",request.url));const base=process.env.SQUARE_ENVIRONMENT==="production"?"https://connect.squareup.com":"https://connect.squareupsandbox.com";const response=await fetch(`${base}/oauth2/token`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({client_id:process.env.SQUARE_APPLICATION_ID,client_secret:process.env.SQUARE_APPLICATION_SECRET,code,grant_type:"authorization_code",redirect_uri:`${process.env.NEXT_PUBLIC_APP_URL}/api/square/callback`})});if(!response.ok)return NextResponse.redirect(new URL("/app/integrations?error=token",request.url));const token=await response.json() as {access_token:string;refresh_token?:string;merchant_id:string;expires_at?:string};const locations=await squareRequestWithToken<{locations:Array<{id:string;status?:string}>}>(token.access_token,"/v2/locations");const location=locations.locations.find(item=>item.status==="ACTIVE")??locations.locations[0];if(!location)return NextResponse.redirect(new URL("/app/integrations?error=location",request.url));await db.transaction(async tx=>{await tx.update(squareOauthStates).set({usedAt:new Date()}).where(and(eq(squareOauthStates.id,record.id),isNull(squareOauthStates.usedAt)));await tx.insert(squareConnections).values({organizationId:record.organizationId,storeId:record.storeId,merchantId:token.merchant_id,locationId:location.id,accessTokenEncrypted:encryptSecret(token.access_token),refreshTokenEncrypted:token.refresh_token?encryptSecret(token.refresh_token):null,tokenExpiresAt:token.expires_at?new Date(token.expires_at):null}).onConflictDoUpdate({target:squareConnections.storeId,set:{merchantId:token.merchant_id,locationId:location.id,accessTokenEncrypted:encryptSecret(token.access_token),refreshTokenEncrypted:token.refresh_token?encryptSecret(token.refresh_token):null,tokenExpiresAt:token.expires_at?new Date(token.expires_at):null,status:"active",updatedAt:new Date()}})});return NextResponse.redirect(new URL("/app/integrations?connected=1",request.url))}
